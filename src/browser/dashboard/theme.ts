import "@fontsource-variable/google-sans-flex/wght.css";
import {createTheme} from "@mui/material";

export const theme = createTheme({
	palette: {
		mode: "light",
	},
	typography: {
		fontFamily: '"Google Sans Flex Variable", sans-serif',
	},
});
